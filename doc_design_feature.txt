{
    "hash_id": {
        "index": 1,
        "hit": 1,
        "favorite": true,
        "ch": "2018-08-05 23:00:00",
        "cd": "2018-08-05 23:00:00",
        "ud": "2018-08-05 23:00:00",
        "word": "wwww",
        "example": [
            "e1",
            "e2"
        ],
    }
}
Json
    $_json_data = ''
    toArray
    toJson
    _updateJsonDataFrom(array) return json_text
    save
Word
    $_word_info = array()
    init($params) return Word
    mergeParams($word_info, $params) return Word
Dictionary
    $_word_info_list = array()

    findByExactWord($word) return word_info
    isExisted($word) return boolean
    addWord($params) return word_info
        add new
        update fields
    editWord($params) return word_info
        edit example
        edit only hit
        edit only favorite
    sortBySortType($data, $sort_type)  return data
        1:
        2:
        3:
    getList($params) return array
        sort_type
    findByTerm($params) return array
        sort_type

DictionaryCli
    check validation input
